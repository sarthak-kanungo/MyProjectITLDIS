import { InsertQueryParamsDirective } from './insert-query-params.directive';

describe('InsertQueryParamsDirective', () => {
  it('should create an instance', () => {
    // const directive = new InsertQueryParamsDirective();
    // expect(directive).toBeTruthy();
  });
});
