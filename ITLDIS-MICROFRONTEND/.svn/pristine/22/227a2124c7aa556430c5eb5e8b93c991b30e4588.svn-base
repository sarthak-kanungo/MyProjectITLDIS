import { TestBed } from '@angular/core/testing';

import { DealerDesignationMasterService } from './dealer-designation-master.service';

describe('DealerDesignationMasterService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: DealerDesignationMasterService = TestBed.get(DealerDesignationMasterService);
    expect(service).toBeTruthy();
  });
});
