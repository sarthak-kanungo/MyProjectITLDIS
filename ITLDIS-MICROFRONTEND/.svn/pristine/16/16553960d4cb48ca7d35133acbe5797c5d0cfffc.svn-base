import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DocumentUploadContainerComponent } from './document-upload-container.component';

describe('DocumentUploadContainerComponent', () => {
  let component: DocumentUploadContainerComponent;
  let fixture: ComponentFixture<DocumentUploadContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DocumentUploadContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DocumentUploadContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
