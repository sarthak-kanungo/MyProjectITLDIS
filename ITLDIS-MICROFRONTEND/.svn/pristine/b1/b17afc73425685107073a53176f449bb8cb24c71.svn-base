import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { IncentiveSchemeClaimApprovalDetailsComponent } from './incentive-scheme-claim-approval-details.component';

describe('IncentiveSchemeClaimApprovalDetailsComponent', () => {
  let component: IncentiveSchemeClaimApprovalDetailsComponent;
  let fixture: ComponentFixture<IncentiveSchemeClaimApprovalDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ IncentiveSchemeClaimApprovalDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(IncentiveSchemeClaimApprovalDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
