import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WpdcComponent } from './wpdc.component';

describe('WpdcComponent', () => {
  let component: WpdcComponent;
  let fixture: ComponentFixture<WpdcComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WpdcComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WpdcComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
