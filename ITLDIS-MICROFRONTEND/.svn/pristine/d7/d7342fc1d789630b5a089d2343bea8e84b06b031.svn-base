import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BranchAccessibleComponent } from './branch-accessible.component';

describe('BranchAccessibleComponent', () => {
  let component: BranchAccessibleComponent;
  let fixture: ComponentFixture<BranchAccessibleComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BranchAccessibleComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BranchAccessibleComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
