import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PickingSlipSearchComponent } from './picking-slip-search.component';

describe('PickingSlipSearchComponent', () => {
  let component: PickingSlipSearchComponent;
  let fixture: ComponentFixture<PickingSlipSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PickingSlipSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PickingSlipSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
