import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NewPartsClaimTableComponent } from './new-parts-claim-table.component';

describe('NewPartsClaimTableComponent', () => {
  let component: NewPartsClaimTableComponent;
  let fixture: ComponentFixture<NewPartsClaimTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NewPartsClaimTableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NewPartsClaimTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
