import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeliveryChallanSearchContainerComponent } from './delivery-challan-search-container.component';

describe('DeliveryChallanSearchContainerComponent', () => {
  let component: DeliveryChallanSearchContainerComponent;
  let fixture: ComponentFixture<DeliveryChallanSearchContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeliveryChallanSearchContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeliveryChallanSearchContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
