import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TransporterSearchComponent } from './transporter-search.component';

describe('TransporterSearchComponent', () => {
  let component: TransporterSearchComponent;
  let fixture: ComponentFixture<TransporterSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TransporterSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TransporterSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
