import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SalesInvoiceCancellationContainerSearchComponent } from './sales-invoice-cancellation-container-search.component';

describe('SalesInvoiceCancellationContainerSearchComponent', () => {
  let component: SalesInvoiceCancellationContainerSearchComponent;
  let fixture: ComponentFixture<SalesInvoiceCancellationContainerSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SalesInvoiceCancellationContainerSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SalesInvoiceCancellationContainerSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
