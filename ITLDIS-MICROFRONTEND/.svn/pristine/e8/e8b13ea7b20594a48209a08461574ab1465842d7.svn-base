import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { JobCardViewModalComponent } from './job-card-view-modal.component';

describe('JobCardViewModalComponent', () => {
  let component: JobCardViewModalComponent;
  let fixture: ComponentFixture<JobCardViewModalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ JobCardViewModalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(JobCardViewModalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
