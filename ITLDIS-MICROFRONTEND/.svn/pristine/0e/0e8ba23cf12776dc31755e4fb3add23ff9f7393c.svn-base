import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ActivityProposalSearchComponent } from './activity-proposal-search.component';

describe('ActivityProposalSearchComponent', () => {
  let component: ActivityProposalSearchComponent;
  let fixture: ComponentFixture<ActivityProposalSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActivityProposalSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActivityProposalSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
