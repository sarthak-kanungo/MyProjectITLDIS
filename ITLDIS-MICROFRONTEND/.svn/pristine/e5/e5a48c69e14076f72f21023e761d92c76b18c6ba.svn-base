import { TestBed } from '@angular/core/testing';

import { RepresentativeDialogWebService } from './representative-dialog-web.service';

describe('RepresentativeDialogWebService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RepresentativeDialogWebService = TestBed.get(RepresentativeDialogWebService);
    expect(service).toBeTruthy();
  });
});
