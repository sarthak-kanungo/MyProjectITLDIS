import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ActivityTypeBudgetComponent } from './activity-type-budget.component';

describe('ActivityTypeBudgetComponent', () => {
  let component: ActivityTypeBudgetComponent;
  let fixture: ComponentFixture<ActivityTypeBudgetComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActivityTypeBudgetComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActivityTypeBudgetComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
