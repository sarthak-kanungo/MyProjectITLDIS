import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LoginImagesComponent } from './login-images.component';

describe('LoginImagesComponent', () => {
  let component: LoginImagesComponent;
  let fixture: ComponentFixture<LoginImagesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LoginImagesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LoginImagesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
