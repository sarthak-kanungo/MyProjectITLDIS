declare module "search-allotment-de-allotment-domain" { 

    export interface ProductDomain {
        product: string;
    }

    export interface EnquiryCodeDomain{
        enquiryCode:string;
    }
    export interface ProductDomain {
        product: string;
    }

    export interface SeriesDomain {
        series: string;
    }
    export interface ModelDomain {
        model: string;
    }
    export interface SubModelDomain {
        subModel: string;
    }
    export interface VariantDomain {
        variant: string;
    }

}