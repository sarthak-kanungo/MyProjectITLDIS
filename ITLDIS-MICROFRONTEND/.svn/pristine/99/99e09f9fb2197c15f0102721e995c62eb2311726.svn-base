import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { KubotaUserSearchResultComponent } from './kubota-user-search-result.component';

describe('KubotaUserSearchResultComponent', () => {
  let component: KubotaUserSearchResultComponent;
  let fixture: ComponentFixture<KubotaUserSearchResultComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ KubotaUserSearchResultComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(KubotaUserSearchResultComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
