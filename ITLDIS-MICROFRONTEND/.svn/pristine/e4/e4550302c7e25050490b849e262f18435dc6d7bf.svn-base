import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { SearchAssignOrgHierarchyToDealerComponent } from './search-assign-org-hierarchy-to-dealer.component';


describe('SearchAssignOrgHierarchyToDealerComponent', () => {
  let component: SearchAssignOrgHierarchyToDealerComponent;
  let fixture: ComponentFixture<SearchAssignOrgHierarchyToDealerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchAssignOrgHierarchyToDealerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchAssignOrgHierarchyToDealerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
