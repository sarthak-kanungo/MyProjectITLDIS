import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchDesignationComponent } from './search-designation.component';

describe('SearchDesignationComponent', () => {
  let component: SearchDesignationComponent;
  let fixture: ComponentFixture<SearchDesignationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchDesignationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchDesignationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
