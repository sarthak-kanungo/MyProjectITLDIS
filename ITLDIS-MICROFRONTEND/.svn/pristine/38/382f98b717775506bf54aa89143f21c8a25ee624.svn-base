import { TestBed } from '@angular/core/testing';

import { SearchComplaintOrQueryResolutionService } from './search-complaint-or-query-resolution.service';

describe('SearchComplaintOrQueryResolutionService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: SearchComplaintOrQueryResolutionService = TestBed.get(SearchComplaintOrQueryResolutionService);
    expect(service).toBeTruthy();
  });
});
