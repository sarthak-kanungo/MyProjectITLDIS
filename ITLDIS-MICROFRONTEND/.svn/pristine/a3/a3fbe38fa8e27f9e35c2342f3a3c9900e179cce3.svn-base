import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchActivityProposalComponent } from './search-activity-proposal.component';

describe('SearchActivityProposalComponent', () => {
  let component: SearchActivityProposalComponent;
  let fixture: ComponentFixture<SearchActivityProposalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchActivityProposalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchActivityProposalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
