import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeliveryChallanReturnSearchContainerComponent } from './delivery-challan-return-search-container.component';

describe('DeliveryChallanReturnSearchContainerComponent', () => {
  let component: DeliveryChallanReturnSearchContainerComponent;
  let fixture: ComponentFixture<DeliveryChallanReturnSearchContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeliveryChallanReturnSearchContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeliveryChallanReturnSearchContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
