import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchActivityClaimComponent } from './search-activity-claim.component';

describe('SearchActivityClaimComponent', () => {
  let component: SearchActivityClaimComponent;
  let fixture: ComponentFixture<SearchActivityClaimComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchActivityClaimComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchActivityClaimComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
