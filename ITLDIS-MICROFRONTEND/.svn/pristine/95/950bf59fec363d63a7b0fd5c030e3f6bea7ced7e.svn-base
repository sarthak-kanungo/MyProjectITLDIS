import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MachineServiceHistoryComponent } from './machine-service-history.component';

describe('MachineServiceHistoryComponent', () => {
  let component: MachineServiceHistoryComponent;
  let fixture: ComponentFixture<MachineServiceHistoryComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MachineServiceHistoryComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MachineServiceHistoryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
