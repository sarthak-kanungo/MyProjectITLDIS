import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PickingSlipContainerComponent } from './picking-slip-container.component';

describe('PickingSlipContainerComponent', () => {
  let component: PickingSlipContainerComponent;
  let fixture: ComponentFixture<PickingSlipContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PickingSlipContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PickingSlipContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
