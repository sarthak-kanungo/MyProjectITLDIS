import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchMtrListComponent } from './search-mtr-list.component';

describe('SearchMtrListComponent', () => {
  let component: SearchMtrListComponent;
  let fixture: ComponentFixture<SearchMtrListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchMtrListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchMtrListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
