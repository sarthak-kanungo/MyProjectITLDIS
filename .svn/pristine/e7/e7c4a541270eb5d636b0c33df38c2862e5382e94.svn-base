import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchDirectSurveyComponent } from './search-direct-survey.component';

describe('SearchDirectSurveyComponent', () => {
  let component: SearchDirectSurveyComponent;
  let fixture: ComponentFixture<SearchDirectSurveyComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SearchDirectSurveyComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchDirectSurveyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
