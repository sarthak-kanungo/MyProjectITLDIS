import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DealerUserSearchComponent } from './dealer-user-search.component';

describe('DealerUserSearchComponent', () => {
  let component: DealerUserSearchComponent;
  let fixture: ComponentFixture<DealerUserSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DealerUserSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DealerUserSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
