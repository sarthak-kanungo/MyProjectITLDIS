import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ChannelFinanceLimitUploadComponent } from './channel-finance-limit-upload.component';

describe('ChannelFinanceLimitUploadComponent', () => {
  let component: ChannelFinanceLimitUploadComponent;
  let fixture: ComponentFixture<ChannelFinanceLimitUploadComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ChannelFinanceLimitUploadComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ChannelFinanceLimitUploadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
