import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { HoursPopUpServiceBookingComponent } from './hours-pop-up-service-booking.component';

describe('HoursPopUpServiceBookingComponent', () => {
  let component: HoursPopUpServiceBookingComponent;
  let fixture: ComponentFixture<HoursPopUpServiceBookingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [HoursPopUpServiceBookingComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(HoursPopUpServiceBookingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
