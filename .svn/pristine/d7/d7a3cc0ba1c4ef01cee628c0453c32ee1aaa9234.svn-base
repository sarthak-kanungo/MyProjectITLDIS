import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TollFreeHistoryComponent } from './toll-free-history.component';

describe('TollFreeHistoryComponent', () => {
  let component: TollFreeHistoryComponent;
  let fixture: ComponentFixture<TollFreeHistoryComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TollFreeHistoryComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TollFreeHistoryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
