import { TestBed } from '@angular/core/testing';

import { SearchExchangeInventoryService } from './search-exchange-inventory.service';

describe('SearchExchangeInventoryService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: SearchExchangeInventoryService = TestBed.get(SearchExchangeInventoryService);
    expect(service).toBeTruthy();
  });
});
