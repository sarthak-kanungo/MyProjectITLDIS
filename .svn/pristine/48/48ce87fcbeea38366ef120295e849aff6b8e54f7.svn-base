import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WpdcMaterialDetailsComponent } from './wpdc-material-details.component';

describe('WpdcMaterialDetailsComponent', () => {
  let component: WpdcMaterialDetailsComponent;
  let fixture: ComponentFixture<WpdcMaterialDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WpdcMaterialDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WpdcMaterialDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
