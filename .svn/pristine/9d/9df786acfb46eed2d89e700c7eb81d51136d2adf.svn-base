import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VehicleRegistrationCustomerDetailsComponent } from './vehicle-registration-customer-details.component';

describe('VehicleRegistrationCustomerDetailsComponent', () => {
  let component: VehicleRegistrationCustomerDetailsComponent;
  let fixture: ComponentFixture<VehicleRegistrationCustomerDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VehicleRegistrationCustomerDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VehicleRegistrationCustomerDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
