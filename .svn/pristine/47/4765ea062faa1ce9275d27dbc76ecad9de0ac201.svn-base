import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ServiceClaimDetailsComponent } from './service-claim-details.component';

describe('ServiceClaimDetailsComponent', () => {
  let component: ServiceClaimDetailsComponent;
  let fixture: ComponentFixture<ServiceClaimDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ServiceClaimDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ServiceClaimDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
