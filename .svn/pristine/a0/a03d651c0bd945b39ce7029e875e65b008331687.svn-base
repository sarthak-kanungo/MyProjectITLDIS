import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeAllotmentCreateComponent } from './de-allotment-create.component';

describe('DeAllotmentCreateComponent', () => {
  let component: DeAllotmentCreateComponent;
  let fixture: ComponentFixture<DeAllotmentCreateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeAllotmentCreateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeAllotmentCreateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
