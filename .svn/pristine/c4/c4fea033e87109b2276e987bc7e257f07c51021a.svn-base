import { TestBed } from '@angular/core/testing';

import { DeliveryChallanCancellationApprovalService } from './delivery-challan-cancellation-approval.service';

describe('DeliveryChallanCancellationApprovalService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: DeliveryChallanCancellationApprovalService = TestBed.get(DeliveryChallanCancellationApprovalService);
    expect(service).toBeTruthy();
  });
});
