import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PickingSlipComponent } from './picking-slip.component';

describe('PickingSlipComponent', () => {
  let component: PickingSlipComponent;
  let fixture: ComponentFixture<PickingSlipComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PickingSlipComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PickingSlipComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
