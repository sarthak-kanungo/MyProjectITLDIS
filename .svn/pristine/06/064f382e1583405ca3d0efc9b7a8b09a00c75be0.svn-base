import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CurrentEnquiryFollowupComponent } from './current-enquiry-followup.component';

describe('CurrentEnquiryFollowupComponent', () => {
  let component: CurrentEnquiryFollowupComponent;
  let fixture: ComponentFixture<CurrentEnquiryFollowupComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CurrentEnquiryFollowupComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CurrentEnquiryFollowupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
