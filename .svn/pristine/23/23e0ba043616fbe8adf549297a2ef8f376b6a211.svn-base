import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WarrentyClaimRequestRemarksComponent } from './warrenty-claim-request-remarks.component';

describe('WarrentyClaimRequestRemarksComponent', () => {
  let component: WarrentyClaimRequestRemarksComponent;
  let fixture: ComponentFixture<WarrentyClaimRequestRemarksComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WarrentyClaimRequestRemarksComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WarrentyClaimRequestRemarksComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
