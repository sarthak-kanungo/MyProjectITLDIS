import { TestBed } from '@angular/core/testing';

import { SaveInvoiceAdaptorService } from './save-invoice-adaptor.service';

describe('SaveInvoiceAdaptorService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: SaveInvoiceAdaptorService = TestBed.get(SaveInvoiceAdaptorService);
    expect(service).toBeTruthy();
  });
});
