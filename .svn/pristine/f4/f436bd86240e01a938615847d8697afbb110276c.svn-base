import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DealerMasterSearchComponent } from './dealer-master-search.component';

describe('DealerMasterSearchComponent', () => {
  let component: DealerMasterSearchComponent;
  let fixture: ComponentFixture<DealerMasterSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DealerMasterSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DealerMasterSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
