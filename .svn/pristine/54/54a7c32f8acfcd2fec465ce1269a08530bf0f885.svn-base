import { TestBed } from '@angular/core/testing';

import { ComplaintOrQueryResolutionService } from './complaint-or-query-resolution.service';

describe('ComplaintOrQueryResolutionService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: ComplaintOrQueryResolutionService = TestBed.get(ComplaintOrQueryResolutionService);
    expect(service).toBeTruthy();
  });
});
