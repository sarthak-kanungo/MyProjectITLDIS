import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DealerMasterCreateComponent } from './dealer-master-create.component';

describe('DealerMasterCreateComponent', () => {
  let component: DealerMasterCreateComponent;
  let fixture: ComponentFixture<DealerMasterCreateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DealerMasterCreateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DealerMasterCreateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
