import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { QuotationContainerComponent } from './quotation-container.component';

describe('QuotationContainerComponent', () => {
  let component: QuotationContainerComponent;
  let fixture: ComponentFixture<QuotationContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ QuotationContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(QuotationContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
