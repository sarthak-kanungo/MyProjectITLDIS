import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartyMasterSearchPageComponent } from './party-master-search-page.component';

describe('PartyMasterSearchPageComponent', () => {
  let component: PartyMasterSearchPageComponent;
  let fixture: ComponentFixture<PartyMasterSearchPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartyMasterSearchPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartyMasterSearchPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
