import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MarketingActivityAddproductComponent } from './marketing-activity-addproduct.component';

describe('MarketingActivityAddproductComponent', () => {
  let component: MarketingActivityAddproductComponent;
  let fixture: ComponentFixture<MarketingActivityAddproductComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MarketingActivityAddproductComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MarketingActivityAddproductComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
