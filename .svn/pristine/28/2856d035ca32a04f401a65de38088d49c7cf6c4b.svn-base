import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ActivityTrainingProposalDetailsComponent } from './activity-training-proposal-details.component';

describe('ActivityTrainingProposalDetailsComponent', () => {
  let component: ActivityTrainingProposalDetailsComponent;
  let fixture: ComponentFixture<ActivityTrainingProposalDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActivityTrainingProposalDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActivityTrainingProposalDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
