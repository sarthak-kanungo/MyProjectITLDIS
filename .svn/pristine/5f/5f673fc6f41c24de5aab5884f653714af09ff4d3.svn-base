import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchDealerDepartmentComponent } from './search-dealer-department.component';

describe('SearchDealerDepartmentComponent', () => {
  let component: SearchDealerDepartmentComponent;
  let fixture: ComponentFixture<SearchDealerDepartmentComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchDealerDepartmentComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchDealerDepartmentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
