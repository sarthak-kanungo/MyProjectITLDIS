import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateNewDealerUserComponent } from './create-new-dealer-user.component';

describe('CreateNewDealerUserComponent', () => {
  let component: CreateNewDealerUserComponent;
  let fixture: ComponentFixture<CreateNewDealerUserComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CreateNewDealerUserComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CreateNewDealerUserComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
