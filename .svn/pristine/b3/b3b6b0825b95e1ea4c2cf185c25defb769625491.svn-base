import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddDesignationLevelComponent } from './add-designation-level.component';

describe('AddDesignationLevelComponent', () => {
  let component: AddDesignationLevelComponent;
  let fixture: ComponentFixture<AddDesignationLevelComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AddDesignationLevelComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AddDesignationLevelComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
