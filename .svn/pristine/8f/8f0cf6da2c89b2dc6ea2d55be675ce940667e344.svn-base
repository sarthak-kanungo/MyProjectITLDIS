import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeliveryChallanCancellationApprovalComponent } from './delivery-challan-cancellation-approval.component';

describe('DeliveryChallanCancellationApprovalComponent', () => {
  let component: DeliveryChallanCancellationApprovalComponent;
  let fixture: ComponentFixture<DeliveryChallanCancellationApprovalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeliveryChallanCancellationApprovalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeliveryChallanCancellationApprovalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
