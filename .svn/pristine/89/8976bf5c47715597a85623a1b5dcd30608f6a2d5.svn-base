import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TransporterMasterCreateComponent } from './transporter-master-create.component';

describe('TransporterMasterCreateComponent', () => {
  let component: TransporterMasterCreateComponent;
  let fixture: ComponentFixture<TransporterMasterCreateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TransporterMasterCreateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TransporterMasterCreateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
