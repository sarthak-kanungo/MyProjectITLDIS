import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MarketingActivityCreateContainerComponent } from './marketing-activity-create-container.component';

describe('MarketingActivityCreateContainerComponent', () => {
  let component: MarketingActivityCreateContainerComponent;
  let fixture: ComponentFixture<MarketingActivityCreateContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MarketingActivityCreateContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MarketingActivityCreateContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
