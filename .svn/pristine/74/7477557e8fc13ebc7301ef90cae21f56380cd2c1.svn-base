import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CurrentMachineDetailsContainerComponent } from './current-machine-details-container.component';

describe('CurrentMachineDetailsContainerComponent', () => {
  let component: CurrentMachineDetailsContainerComponent;
  let fixture: ComponentFixture<CurrentMachineDetailsContainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CurrentMachineDetailsContainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CurrentMachineDetailsContainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
