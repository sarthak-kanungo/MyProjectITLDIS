import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WholesaleIncentiveSchemeDetailsComponent } from './wholesale-incentive-scheme-details.component';

describe('WholesaleIncentiveSchemeDetailsComponent', () => {
  let component: WholesaleIncentiveSchemeDetailsComponent;
  let fixture: ComponentFixture<WholesaleIncentiveSchemeDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WholesaleIncentiveSchemeDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WholesaleIncentiveSchemeDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
