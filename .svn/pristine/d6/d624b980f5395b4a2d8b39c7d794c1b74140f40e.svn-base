import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartReturnPageComponent } from './part-return-page.component';

describe('PartReturnPageComponent', () => {
  let component: PartReturnPageComponent;
  let fixture: ComponentFixture<PartReturnPageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartReturnPageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartReturnPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
