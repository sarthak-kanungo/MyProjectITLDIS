import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SpareDeliveryChallanComponent } from './spare-delivery-challan.component';

describe('SpareDeliveryChallanComponent', () => {
  let component: SpareDeliveryChallanComponent;
  let fixture: ComponentFixture<SpareDeliveryChallanComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SpareDeliveryChallanComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SpareDeliveryChallanComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
