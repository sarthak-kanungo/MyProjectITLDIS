import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PdcChecklistComponent } from './pdc-checklist.component';

describe('PdcChecklistComponent', () => {
  let component: PdcChecklistComponent;
  let fixture: ComponentFixture<PdcChecklistComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PdcChecklistComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PdcChecklistComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
