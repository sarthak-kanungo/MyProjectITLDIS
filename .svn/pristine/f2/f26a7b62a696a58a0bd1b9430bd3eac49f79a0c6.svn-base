import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchOrderPlanningSheetComponent } from './search-order-planning-sheet.component';

describe('SearchOrderPlanningSheetComponent', () => {
  let component: SearchOrderPlanningSheetComponent;
  let fixture: ComponentFixture<SearchOrderPlanningSheetComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchOrderPlanningSheetComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchOrderPlanningSheetComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
