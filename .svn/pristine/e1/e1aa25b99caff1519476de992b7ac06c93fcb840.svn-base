import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartReturnSearchComponent } from './part-return-search.component';

describe('PartReturnSearchComponent', () => {
  let component: PartReturnSearchComponent;
  let fixture: ComponentFixture<PartReturnSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartReturnSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartReturnSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
