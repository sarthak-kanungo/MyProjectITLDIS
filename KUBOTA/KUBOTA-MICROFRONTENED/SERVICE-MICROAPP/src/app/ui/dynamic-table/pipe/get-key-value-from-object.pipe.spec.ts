import { GetKeyValueFromObjectPipe } from './get-key-value-from-object.pipe';

describe('GetKeyValueFromObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new GetKeyValueFromObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
