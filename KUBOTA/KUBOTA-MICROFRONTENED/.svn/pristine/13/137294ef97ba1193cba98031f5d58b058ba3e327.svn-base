import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchDeliveryChallanComponent } from './search-delivery-challan.component';

describe('SearchDeliveryChallanComponent', () => {
  let component: SearchDeliveryChallanComponent;
  let fixture: ComponentFixture<SearchDeliveryChallanComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchDeliveryChallanComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchDeliveryChallanComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
