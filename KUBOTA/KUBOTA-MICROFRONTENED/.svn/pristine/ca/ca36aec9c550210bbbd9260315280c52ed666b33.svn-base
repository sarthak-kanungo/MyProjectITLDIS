import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { KubotaImplementComponent } from './kubota-implement.component';

describe('KubotaImplementComponent', () => {
  let component: KubotaImplementComponent;
  let fixture: ComponentFixture<KubotaImplementComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ KubotaImplementComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(KubotaImplementComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
