import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReInstallationDetailsComponent } from './re-installation-details.component';

describe('ReInstallationDetailsComponent', () => {
  let component: ReInstallationDetailsComponent;
  let fixture: ComponentFixture<ReInstallationDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReInstallationDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReInstallationDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
