import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DestinationPortsComponent } from './destination-ports.component';

describe('DestinationPortsComponent', () => {
  let component: DestinationPortsComponent;
  let fixture: ComponentFixture<DestinationPortsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DestinationPortsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DestinationPortsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
