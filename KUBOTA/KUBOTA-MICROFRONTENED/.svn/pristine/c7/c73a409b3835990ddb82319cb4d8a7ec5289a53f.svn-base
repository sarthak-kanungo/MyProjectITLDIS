import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchActivityBudgetComponent } from './search-activity-budget.component';

describe('SearchActivityBudgetComponent', () => {
  let component: SearchActivityBudgetComponent;
  let fixture: ComponentFixture<SearchActivityBudgetComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchActivityBudgetComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchActivityBudgetComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
