import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WpdcSearchComponent } from './wpdc-search.component';

describe('WpdcSearchComponent', () => {
  let component: WpdcSearchComponent;
  let fixture: ComponentFixture<WpdcSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WpdcSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WpdcSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
