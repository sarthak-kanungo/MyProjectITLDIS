import { TestBed } from '@angular/core/testing';

import { ProspectBackgroundContainerService } from './prospect-background-container.service';

describe('ProspectBackgroundContainerService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: ProspectBackgroundContainerService = TestBed.get(ProspectBackgroundContainerService);
    expect(service).toBeTruthy();
  });
});
