<!-- <div class="table-responsive mt-1">
  <table class="table w-50 table-bordered table-sm">
    <thead class="thead-light">
      <tr>
        <th scope="col">Module</th>
        <th scope="col">Functionality</th>
        <th scope="col">Assigned in Role</th>
        <th scope="col">Is Accessible</th>
      </tr>
      <tr></tr>
    </thead>
    <tbody>
      <tr *ngFor="let master of functionalityMasters" [ngClass]="{'bg-light' : master?.isSpoiled, 'text-muted' : master?.isSpoiled}">
        <td>{{master?.module}}</td>
        <td>{{master?.functionality}}</td>
        <td>{{master?.assignedInRole}}</td>
        <td> <mat-checkbox color="basic" [checked]="master?.accessibleFlag" (change)="onAccessiblityChange($event, master)"></mat-checkbox> </td>
      </tr>
    </tbody>
  </table>
</div> -->

<div *ngIf="functionalityMasters" [formGroup]="menuGroup">
<mat-tree [dataSource]="dataSource" [treeControl]="treeControl">
  <!-- This is the tree node template for leaf nodes -->
  <mat-tree-node *matTreeNodeDef="let node" matTreeNodePadding>
    <!-- use a disabled button to provide padding for tree leaf -->
    <button mat-icon-button ></button>
    <mat-checkbox class="checklist-leaf-node"
                [value]="node.id"
                [disabled]="isView"
                [checked]="checklistSelection.isSelected(node)"
                (change)="checklistSelection.toggle(node);">{{node.name}}</mat-checkbox>
  </mat-tree-node>
  <!-- This is the tree node template for expandable nodes -->
  <mat-tree-node *matTreeNodeDef="let node;when: hasChild" matTreeNodePadding>
    <button mat-icon-button matTreeNodeToggle
            [attr.aria-label]="'toggle ' + node.name">
      <mat-icon class="mat-icon-rtl-mirror">
        {{treeControl.isExpanded(node) ? 'expand_more' : 'chevron_right'}}
      </mat-icon>
    </button>
    <mat-checkbox [checked]="descendantsAllSelected(node)"
                [value]="node.id"
                [disabled]="isView"
                [indeterminate]="descendantsPartiallySelected(node)"
                (change)="todoItemSelectionToggle(node);">{{node.name}}</mat-checkbox>
  </mat-tree-node>
</mat-tree>
</div>