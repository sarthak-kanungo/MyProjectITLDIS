import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeliveryChallanSearchComponent } from './delivery-challan-search.component';

describe('DeliveryChallanSearchComponent', () => {
  let component: DeliveryChallanSearchComponent;
  let fixture: ComponentFixture<DeliveryChallanSearchComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeliveryChallanSearchComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeliveryChallanSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
