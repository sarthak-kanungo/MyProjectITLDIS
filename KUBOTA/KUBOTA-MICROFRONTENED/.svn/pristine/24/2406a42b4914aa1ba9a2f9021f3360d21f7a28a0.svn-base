import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchMachineTransferRequestComponent } from './search-machine-transfer-request.component';

describe('SearchMachineTransferRequestComponent', () => {
  let component: SearchMachineTransferRequestComponent;
  let fixture: ComponentFixture<SearchMachineTransferRequestComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchMachineTransferRequestComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchMachineTransferRequestComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
