import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateMtrListApprovalComponent } from './create-mtr-list-approval.component';

describe('CreateMtrListApprovalComponent', () => {
  let component: CreateMtrListApprovalComponent;
  let fixture: ComponentFixture<CreateMtrListApprovalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CreateMtrListApprovalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CreateMtrListApprovalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
