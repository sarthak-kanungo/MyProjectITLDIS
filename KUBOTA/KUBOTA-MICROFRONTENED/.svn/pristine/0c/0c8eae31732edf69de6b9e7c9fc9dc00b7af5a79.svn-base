import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchKubotaEmpolyeeComponent } from './search-kubota-empolyee.component';

describe('SearchKubotaEmpolyeeComponent', () => {
  let component: SearchKubotaEmpolyeeComponent;
  let fixture: ComponentFixture<SearchKubotaEmpolyeeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchKubotaEmpolyeeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchKubotaEmpolyeeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
