import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchServiceClaimComponent } from './search-service-claim.component';

describe('SearchServiceClaimComponent', () => {
  let component: SearchServiceClaimComponent;
  let fixture: ComponentFixture<SearchServiceClaimComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchServiceClaimComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchServiceClaimComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
