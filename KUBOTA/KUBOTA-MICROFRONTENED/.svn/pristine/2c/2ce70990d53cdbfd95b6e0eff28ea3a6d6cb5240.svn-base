import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AssignFunctionComponent } from './assign-function.component';

describe('AssignFunctionComponent', () => {
  let component: AssignFunctionComponent;
  let fixture: ComponentFixture<AssignFunctionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AssignFunctionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AssignFunctionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
