import { TestBed } from '@angular/core/testing';

import { CurrentMachineDetailsContainerService } from './current-machine-details-container.service';

describe('CurrentMachineDetailsContainerService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CurrentMachineDetailsContainerService = TestBed.get(CurrentMachineDetailsContainerService);
    expect(service).toBeTruthy();
  });
});
