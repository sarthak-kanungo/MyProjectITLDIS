import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LogSheetDealerComponent } from './log-sheet-dealer.component';

describe('LogSheetDealerComponent', () => {
  let component: LogSheetDealerComponent;
  let fixture: ComponentFixture<LogSheetDealerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LogSheetDealerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LogSheetDealerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
