import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ProspectBackgroundComponent } from './prospect-background.component';

describe('ProspectBackgroundComponent', () => {
  let component: ProspectBackgroundComponent;
  let fixture: ComponentFixture<ProspectBackgroundComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ProspectBackgroundComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ProspectBackgroundComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
