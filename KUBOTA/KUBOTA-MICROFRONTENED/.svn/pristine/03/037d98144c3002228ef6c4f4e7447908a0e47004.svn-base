import { TestBed } from '@angular/core/testing';

import { ServiceActivityClaimHeadsWebService } from './service-activity-claim-heads-web.service';

describe('ServiceActivityClaimHeadsWebService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: ServiceActivityClaimHeadsWebService = TestBed.get(ServiceActivityClaimHeadsWebService);
    expect(service).toBeTruthy();
  });
});
