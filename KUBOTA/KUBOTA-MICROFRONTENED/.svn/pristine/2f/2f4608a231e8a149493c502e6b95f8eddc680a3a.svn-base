import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartReturnTableComponent } from './part-return-table.component';

describe('PartReturnTableComponent', () => {
  let component: PartReturnTableComponent;
  let fixture: ComponentFixture<PartReturnTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartReturnTableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartReturnTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
