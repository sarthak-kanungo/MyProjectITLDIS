import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeliveryChallanCancelComponent } from './delivery-challan-cancel.component';

describe('DeliveryChallanCancelComponent', () => {
  let component: DeliveryChallanCancelComponent;
  let fixture: ComponentFixture<DeliveryChallanCancelComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeliveryChallanCancelComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeliveryChallanCancelComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
