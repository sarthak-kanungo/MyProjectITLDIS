import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CallAttemptDetailsComponent } from './call-attempt-details.component';

describe('CallAttemptDetailsComponent', () => {
  let component: CallAttemptDetailsComponent;
  let fixture: ComponentFixture<CallAttemptDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CallAttemptDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CallAttemptDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
