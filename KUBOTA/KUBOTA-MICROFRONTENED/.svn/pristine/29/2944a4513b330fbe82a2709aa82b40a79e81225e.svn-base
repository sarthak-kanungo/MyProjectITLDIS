import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateBintobinTransferComponent } from './create-bintobin-transfer.component';

describe('CreateBintobinTransferComponent', () => {
  let component: CreateBintobinTransferComponent;
  let fixture: ComponentFixture<CreateBintobinTransferComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CreateBintobinTransferComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CreateBintobinTransferComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
