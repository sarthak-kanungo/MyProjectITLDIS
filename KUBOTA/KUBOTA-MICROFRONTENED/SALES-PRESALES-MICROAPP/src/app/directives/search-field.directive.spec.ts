import { SearchFieldDirective } from './search-field.directive';

describe('SearchFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchFieldDirective();
    expect(directive).toBeTruthy();
  });
});
